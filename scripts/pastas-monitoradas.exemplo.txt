# Pastas que o dashboard vai monitorar — uma por linha.
#
# SINTAXE:  CAMINHO DA PASTA = base1, base2, base3
#
# O "= base" fixa para qual base do dashboard os arquivos daquela pasta
# vão — é o mesmo efeito de escolher a aba na tela de Atualização de Dados.
# Uma pasta pode alimentar VÁRIAS bases. Cada arquivo físico é examinado uma
# vez e somente as abas compatíveis são carregadas.
#
# Bases aceitas: termos, faturamento, vendas, implantacao, programacao,
#                faturamento_implantacao, metas, atendimento
# Sem "= base", o sistema tenta identificar pelas colunas do arquivo.
#
# Subpastas entram automaticamente. Linhas com # são ignoradas.
#
# Funciona com OneDrive/Google Drive: a pasta sincronizada é uma pasta
# normal no PC, basta apontar para ela. Arquivos "somente na nuvem" são
# baixados sozinhos na leitura (a primeira execução pode demorar mais).

# ---------------------------------------------------------------------
# CONFIGURAÇÃO PRONTA — OneDrive AEGEA, pasta "DashBoard - Interior"
# Substitua <PASTA_ONEDRIVE> pela pasta sincronizada do seu computador e
# descomente as linhas abaixo removendo o # do começo.
# ---------------------------------------------------------------------

# <PASTA_ONEDRIVE>\DashBoard - Interior\Interior = vendas, implantacao, termos
# <PASTA_ONEDRIVE>\DashBoard - Interior\Faturamento = faturamento, faturamento_implantacao
# <PASTA_ONEDRIVE>\DashBoard - Interior\Programação Diaria = programacao

# Atendimento ("Analitico - acomp de solicitação"): entra como Vendas por
# Outros Canais, aplicando a mesma regra da medida do Power BI (só as
# solicitações executadas de implantação de ligação de água, nas
# localidades do Interior). O arquivo tem ~153 mil linhas e é lido em
# blocos automaticamente — só leve em conta que demora mais que os demais.
# <PASTA_ONEDRIVE>\DashBoard - Interior\Atendimento = atendimento
