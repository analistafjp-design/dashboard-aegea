# Copie para .env e ajuste conforme o ambiente.
# Todas as variáveis são opcionais — o sistema roda sem nenhuma delas.

# Nome exibido no cabeçalho
APP_NOME=Dashboard Executivo

# Logs detalhados (true apenas em desenvolvimento)
DEBUG=false

# Banco de dados.
# SQLite (padrão):     sqlite:///./data/database/dashboard.db
# PostgreSQL:          postgresql+psycopg://usuario:senha@servidor:5432/dashboard
DATABASE_URL=sqlite:///./data/database/dashboard.db

# Pasta de dados (uploads, arquivos processados, banco e logs)
DATA_DIR=./data

# Limite de upload em megabytes
TAMANHO_MAXIMO_MB=50

# Limite de linhas por arquivo — proteção contra estourar a memória da
# instância (ver docs/deploy.md, seção "Sobre memória"). Aumente se o
# servidor tiver mais RAM disponível.
LIMITE_LINHAS_ARQUIVO=100000

# Tamanho de cada bloco usado para Excel acima do limite de linhas.
LINHAS_POR_BLOCO=20000

# Tempo de vida do cache de indicadores, em segundos.
# O cache também é invalidado automaticamente a cada importação.
CACHE_TTL=300

# Login HTTP Basic — opcional. Sem as duas variáveis preenchidas, o
# sistema roda sem senha (padrão em desenvolvimento local). Preencha as
# duas sempre que a URL for acessível por outras pessoas, mesmo que só
# na rede interna — ver docs/deploy.md.
AUTH_USUARIO=
AUTH_SENHA=
